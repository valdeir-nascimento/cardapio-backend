meta {
  name: Delete Category
  type: http
  seq: 6
}

delete {
  url: {{baseUrl}}/api/v1/admin/categories/{{categoryId}}
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

docs {
  Falha com 422 (`CATEGORY_HAS_PRODUCTS`) se houver produtos vinculados.
}
