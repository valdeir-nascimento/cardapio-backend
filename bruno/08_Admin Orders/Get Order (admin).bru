meta {
  name: Get Order (admin)
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/api/v1/admin/orders/{{orderId}}
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

docs {
  Admin pode ver qualquer pedido. Resposta inclui itens snapshot, endereço e timestamps.
}
