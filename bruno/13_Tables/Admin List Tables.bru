meta {
  name: Admin List Tables
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/v1/admin/tables
  body: none
  auth: bearer
}

auth:bearer {
  token: {{adminAccessToken}}
}

docs {
  Lista todas as mesas com flag `hasOpenComanda` derivada.
}
