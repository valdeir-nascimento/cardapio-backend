meta {
  name: Get Payment
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/v1/payments/{{paymentId}}
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

docs {
  Retorna o status atual + QR / cartão. Usuário só vê seu próprio pagamento (404 caso contrário).
}
